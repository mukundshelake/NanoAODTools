name: analysis
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pyyaml>=6.0
  - numpy>=1.24
  - pandas>=2.0
  - matplotlib>=3.7
  - scipy>=1.10
  - scikit-learn>=1.3
  - jupyterlab>=4.0
  - pip
  - pip:
    - uproot  # For reading ROOT files
    - awkward  # Columnar arrays
    - hist    # Histogramming
